{% extends 'base.html.twig' %}

{% block title %}Ajouter une préstation{% endblock %}

{% block body %}
          {{ form_start(form) }}
          {{ form_errors(form) }}
               <div class="container">
                    <div class="row">
                         <div class="col-4 mb-4 mt-4">
                              {{ form_row(form.title, { 'label': 'Titre' }) }}
                         </div>
                         <div class="col-4 mb-4 mt-4">
                              {{ form_row(form.description) }}
                         </div>
                         <div class="col-4 mb-4">   
                              {{ form_row(form.price, { 'label': 'Prix' }) }}
                         </div>
                         <div class="col-4">        
                              {{ form_row(form.img) }}
                         </div>
                         <hr>
                         <h2>Ajout de photos pour la gallerie d'image </h2>
                         <div class="col-8">        
                              {{ form_row(form.imgCollectionBenefits) }}
                         </div>
                         {# on va construire dynamiquement la liste des modules existants à partire de Programme #}
                         {# <div class="col-6 mb-4 mt-4">
                              <div class="form-group row ">
                                   <div class="col-form-label col-sm-2">&nbsp;</div>
                                   <div id="programme-fields-list" class="col-sm-10 remove-collection-widget"
                                        data-prototype="{{ form_widget(form.imgCollectionBenefits.vars.prototype)|e }}"
                                        data-session="{{sessionId}}"
                                        data-widget-tags="{{ '<p></p>'|e }}"
                                        data-widget-counter="{{ form.imgCollectionBenefits|length }}">
                                        {% for programmeField in form.imgCollectionBenefits %}
                                        <p>
                                             {{ form_errors(programmeField) }}
                                             {{ form_widget(programmeField, {'attr': {'class': 'borders'}}) }}
                                        </p>
                                        {% endfor %}
                                   </div>
                              </div>
                             <div class="form-group row flex-action">
                                   <button type="button"
                                        class="add-another-collection-widget button"
                                        data-list-selector="#programme-fields-list">Ajouter un nouveau module
                                   </button>
                             </div>
                             <div class="borders"></div>
                              empêche l'apparition d'une légende "programmes", si la collection est vide (en cas de création d'une nouvelle session)
                             {% do form.imgCollectionBenefits.setRendered %}
                         </div>  #}
                         <div class="row">
                              <div class="col-4 mt-4">   
                                   {{ form_row(form.submit, { 'label': 'Envoyer' }) }}
                              </div>
                         </div>     
                    </div>   
               </div> 
     {{ form_end(form) }}
{% endblock %}





