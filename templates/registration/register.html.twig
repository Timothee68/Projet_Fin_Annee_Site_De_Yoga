{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    {% if edit %}
    <h1 class="text-center mb-4" >Modifification de votre compte</h1>
    {% else %}
    <h1 class="text-center mt-5 mb-4">Crée un compte</h1>
    {% endif %}
   

    {{ form_start(registrationForm) }}
    <div class="container">
        <div class="register mb-5">
            <div class="row ">
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.name) }}
                </div>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.firstName) }}
                </div>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.dateOfBirth) }}
                </div>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.email) }}
                </div>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.telephone) }}
                </div>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.img) }}
                </div>
                <hr>
                <div class="col-6 mb-4 mt-4">
                    {{ form_row(registrationForm.plainPassword, {
                        label: 'Password'
                    }) }}
                </div>
                <div class="row">
                    <div class="col-6 mb-4 mt-4">
                        {{ form_row(registrationForm.agreeTerms) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-6 mb-4 mt-4">
                        {{ form_row(registrationForm.recaptcha) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <button type="submit" class="btn">Register</button>
                        {{ form_end(registrationForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
